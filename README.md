## 1. Основы и переменные
```kotlin
val a = 10      // Нельзя изменить (Value)
var b = 5       // Можно изменить (Variable)

// Базовые типы (пишутся с большой буквы)
val i: Int = 10
val d: Double = 1.5
val f: Float = 1.5f
val s: String = "Hello"
val b: Boolean = true
```

## 2. Null Safety (Твоя защита от ошибок)
```kotlin
var name: String? = null // Может быть null

name?.length             // Безопасный вызов (вернет length или null)
name ?: "Пусто"          // Элвис-оператор: если null, то вернет "Пусто"
name!!.length            // Умри, но достань (вызовет ошибку, если null)

val len = name?.length ?: 0 // Классическая связка
```

## 3. Управляющие конструкции
```kotlin
// IF как выражение (в Котлине нет тернарного оператора ? :)
val result = if (a > b) "Больше" else "Меньше"

// WHEN (умный switch)
val text = when (x) {
    1 -> "Один"
    in 2..10 -> "От 2 до 10"
    is String -> "Это строка"
    else -> "Что-то другое"
}

// ЦИКЛЫ
for (i in 1..5) { }          // 1, 2, 3, 4, 5
for (i in 1 until 5) { }     // 1, 2, 3, 4
for (i in 5 downTo 1) { }    // 5, 4, 3, 2, 1
for (item in list) { }       // Проход по коллекции
```

## 4. Коллекции и Массивы (Очень важно на задачах!)
```kotlin
// МАССИВЫ (фиксированный размер)
val arr = arrayOf(1, 2, 3)
arr[0] = 10

// СПИСКИ
val readOnly = listOf(1, 2, 3)               // Нельзя менять
val mutable = mutableListOf(1, 2, 3)         // Можно менять
mutable.add(4)

// СОРТИРОВКА
mutable.sort()                               // По возрастанию
mutable.sortBy { it.age }                    // Сортировка объектов по полю "age"
val sorted = mutable.sortedByDescending { it.name } // Новый список по убыванию

// ПОИСК И ФИЛЬТРАЦИЯ
val f = mutable.filter { it > 2 }            // Отфильтровать
val m = mutable.map { it * 2 }               // Преобразовать каждый элемент
val found = mutable.find { it == 3 }         // Найти первый попавшийся
val exists = mutable.any { it > 10 }         // Есть ли хоть один > 10?
```

## 5. Строки и Преобразования
```kotlin
val s = "Kotlin"
println("Результат: $s и длина ${s.length}") // Шаблоны строк

val num = "123".toInt()                      // В число
val safeNum = "123a".toIntOrNull() ?: 0      // Безопасное превращение

// Сборка длинных строк
val sb = StringBuilder()
sb.append("Start")
sb.append("-End")
val final = sb.toString()
```

## 6. Функции
```kotlin
// Обычная
fun sum(a: Int, b: Int): Int {
    return a + b
}

// Однострочная
fun multiply(a: Int, b: Int) = a * b

// С параметром по умолчанию
fun greet(name: String = "User") = "Hello, $name"
```

## 7. Классы и ООП
```kotlin
// Хранилище данных (data создает toString, equals, copy автоматически)
data class User(val id: Int, var name: String)

// Обычный класс
open class Animal(val name: String) // open - чтобы можно было наследоваться

class Dog(name: String) : Animal(name) {
    lateinit var breed: String  // Инициализирую позже
}

// Объект-одиночка (Singleton)
object MyConfig {
    val API_KEY = "12345"
}
```

## 8. Scope-функции (Помогают в Android)
| Функция | Обращение | Возвращает | Для чего |
| :--- | :--- | :--- | :--- |
| **apply** | `this` | Сам объект | Настройка (textView.apply { text = "..." }) |
| **let** | `it` | Результат строки | Проверка на null (`obj?.let { ... }`) |
| **with** | `this` | Результат строки | Когда надо много вызвать у одного объекта |
| **run** | `this` | Результат строки | Настройка + расчет результата |

## 9. Математика (может пригодиться)
```kotlin
import kotlin.math.*

val m = max(10, 20)
val n = min(10, 20)
val r = round(10.5)
val p = pow(2.0, 3.0) // 2 в кубе
val a = abs(-10)      // модуль
```

## 10. Ввод данных (Если нужно читать из консоли)
```kotlin
import java.util.Scanner
val scanner = Scanner(System.`in`)
val word = scanner.next()    // Слово
val line = scanner.nextLine() // Строка
val num = scanner.nextInt()   // Число
```

## 11. Сложные коллекции (Maps и Set)
Если нужно считать количество предметов или хранить уникальные значения.
```kotlin
// MAP (Ключ -> Значение)
val map = mutableMapOf<String, Int>()
map["Apple"] = 10              // Добавить
val count = map["Apple"] ?: 0  // Взять (если нет, то 0)

for ((key, value) in map) {    // Перебор карты
    println("$key стоит $value")
}

// SET (Только уникальные элементы)
val numbers = mutableSetOf(1, 2, 2, 3) // Результат: [1, 2, 3]
```

## 12. Продвинутые операции со списками (Для задач)
```kotlin
val list = listOf(1, 2, 3, 4, 5, 6)

val groups = list.groupBy { it % 2 == 0 } // Группировка: {false=[1, 3, 5], true=[2, 4, 6]}
val distinct = list.distinct()            // Удалить дубликаты
val chunked = list.chunked(2)             // Разбить на пары: [[1,2], [3,4], [5,6]]

// Ограничение числа
val x = 150
val clamped = x.coerceIn(0, 100)          // Результат: 100 (не даст выйти за границы)
```

## 13. Работа с индексами
Иногда нужно знать не только элемент, но и его номер:
```kotlin
val fruits = listOf("Apple", "Banana", "Cherry")

for ((index, fruit) in fruits.withIndex()) {
    println("Фрукт №$index - это $fruit")
}
```

## 14. Сортировка объектов (Важно!)
Если есть класс `User(val name: String, val score: Int)` и нужно отсортировать список таких пользователей:
```kotlin
// Сначала по очкам (убывание), потом по имени (возрастание)
val sortedUsers = users.sortedWith(compareByDescending<User> { it.score }.thenBy { it.name })
```

## 15. Форматирование вывода (Числа)
Чтобы вывести `3.14` вместо `3.1415926` или добавить нули перед числом:
```kotlin
val pi = 3.14159
println("%.2f".format(pi)) // Выведет 3,14

val num = 5
println("%03d".format(num)) // Выведет 005 (полезно для времени/дат)
```

## 16. Работа с Ошибками (Try-Catch)
Чтобы программа не вылетала, если вводят плохие данные:
```kotlin
try {
    val input = scanner.nextInt()
} catch (e: Exception) {
    println("Ошибка! Введи число.")
}
```

## 17. Работа с файлами (Assets)
Часто на конкурсах дают готовый файл (JSON, TXT) с данными, который нужно прочитать.

1.  Создай папку `assets`: **ПКМ на папке `app` -> New -> Folder -> Assets Folder**.
2.  Положи туда файл, например `data.txt`.
3.  Код для чтения:
```kotlin
// Читаем весь файл в строку
val content = context.assets.open("data.txt").bufferedReader().use { it.readText() }

// Читаем построчно в список
val lines = context.assets.open("data.txt").bufferedReader().readLines()
```

## 18. View Binding (Как обращаться к кнопкам без findViewById)
Это современный стандарт. Чтобы не писать каждый раз `findViewById`, настрой Binding.

**В файле `build.gradle.kts (Module :app)`:**
```kotlin
android {
    ...
    viewBinding {
        enable = true
    }
}
```
**В Activity:**
```kotlin
class MainActivity : AppCompatActivity() {
    private lateinit var binding: ActivityMainBinding // Название зависит от XML

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        binding = ActivityMainBinding.inflate(layoutInflater)
        setContentView(binding.root)

        // Теперь просто:
        binding.myButton.text = "Нажми меня"
        binding.myTextView.setOnClickListener { ... }
    }
}
```

## 19. Как подключить библиотеку (БЕЗ интернета)
Если у тебя есть библиотека в виде файла `.jar` или `.aar`:

1.  Скопируй файл в папку `app/libs`.
2.  В `build.gradle.kts (Module :app)` добавь:
```kotlin
dependencies {
    implementation(files("libs/название_файла.jar"))
}
```
3.  Нажми **"Sync Now"** (слоник сверху).

## 20. Переход между экранами (Intents)
```kotlin
// 1. Простой переход
val intent = Intent(this, SecondActivity::class.java)
startActivity(intent)

// 2. Переход с передачей данных
val intent = Intent(this, SecondActivity::class.java).apply {
    putExtra("USER_ID", 123)
    putExtra("USER_NAME", "Ivan")
}
startActivity(intent)

// 3. Как получить данные на втором экране
val id = intent.getIntExtra("USER_ID", 0)
val name = intent.getStringExtra("USER_NAME")
```

## 21. Работа с Датой и Временем
На конкурсах часто просят посчитать разницу во времени.
```kotlin
import java.util.*
import java.text.SimpleDateFormat

// Текущее время в мс
val now = System.currentTimeMillis()

// Форматирование даты в строку
val sdf = SimpleDateFormat("dd.MM.yyyy HH:mm", Locale.getDefault())
val dateStr = sdf.format(Date()) // "07.10.2023 15:30"

// Таймер (выполнить через 2 секунды)
Handler(Looper.getMainLooper()).postDelayed({
    // Твой код здесь
}, 2000)
```

## 22. Потоки (Coroutines) — База
Если нужно сделать что-то «тяжелое», чтобы экран не завис.
```kotlin
import kotlinx.coroutines.*

// Запуск фоновой задачи
GlobalScope.launch(Dispatchers.IO) {
    // Делаем что-то долгое (например, считаем файл)
    
    withContext(Dispatchers.Main) {
        // Возвращаемся в главный поток, чтобы обновить UI (текст, кнопки)
        binding.resultText.text = "Готово!"
    }
}
```

### 23. Настройки Gradle (Чтобы всё работало)
Если проект создается с нуля, проверь эти строки в `build.gradle.kts (Module: app)`:

```kotlin
plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    id("kotlin-kapt") // Нужно для Room!
}

android {
    buildFeatures {
        viewBinding = true // Чтобы работал биндинг
    }
}

dependencies {
    // Библиотеки из твоего скрина (версии могут быть чуть другими)
    implementation("com.github.bumptech.glide:glide:4.16.0") // Картинки
    implementation("com.squareup.retrofit2:retrofit:2.9.0") // Сеть
    implementation("com.squareup.retrofit2:converter-gson:2.9.0") // JSON
    implementation("androidx.room:room-runtime:2.6.1") // База данных
    implementation("androidx.room:room-ktx:2.6.1")
    kapt("androidx.room:room-compiler:2.6.1")
}
```

## 24. Список (RecyclerView) — шаблон Адаптера
Это самая сложная часть, которую все забывают. Скопируй этот шаблон:

```kotlin
class ProductAdapter : RecyclerView.Adapter<ProductAdapter.Holder>() {
    var items: List<Product> = emptyList()
        set(value) {
            field = value
            notifyDataSetChanged() // Обновить список
        }

    // Хранит ссылки на элементы дизайна одного пункта списка
    class Holder(val binding: ItemProductBinding) : RecyclerView.ViewHolder(binding.root)

    override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): Holder {
        val binding = ItemProductBinding.inflate(LayoutInflater.from(parent.context), parent, false)
        return Holder(binding)
    }

    override fun onBindViewHolder(holder: Holder, position: Int) {
        val item = items[position]
        with(holder.binding) {
            tvTitle.text = item.title
            tvPrice.text = item.price.toString()
            // Glide для загрузки картинок:
            // Glide.with(root.context).load(item.imageUrl).into(ivProduct)
        }
    }

    override fun getItemCount() = items.size
}
```

## 25. Сеть (Retrofit) — шаблон запроса
```kotlin
// 1. Описываем запросы
interface ApiService {
    @GET("photos")
    suspend fun getPhotos(): List<PhotoDto>
}

// 2. Создаем клиент (RetrofitInstance)
object RetrofitInstance {
    private const val BASE_URL = "https://jsonplaceholder.typicode.com/"

    val api: ApiService by lazy {
        Retrofit.Builder()
            .baseUrl(BASE_URL)
            .addConverterFactory(GsonConverterFactory.create())
            .build()
            .create(ApiService::class.java)
    }
}
```

## 26. База данных (Room) — шаблон DAO
Если нужно сохранять данные в телефон:

```kotlin
@Dao
interface PhotoDao {
    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insertPhotos(photos: List<PhotoEntity>)

    @Query("SELECT * FROM photos_table")
    suspend fun getAllPhotos(): List<PhotoEntity>
}
```

Вот дополнение к твоей шпаргалке, основанное именно на тексте конкурсного задания:

## 27. Архитектура и Комментарии (Модуль А)
В задании требуют комментарии в каждом классе. Создай шаблон в голове или в файле:
```kotlin
/**
 * Назначение: [Например: Главный экран приложения]
 * Дата создания: 07.10.2023
 * Автор: Физи (Fizzy423)
 */
```

## 28. Валидация Email и Пароля (Модуль Б)
Задание требует строгий паттерн: `name@domenname.ru` (только строчные буквы и цифры) и пароль (8+ символов, заглавные, строчные, спецсимволы).

```kotlin
fun isEmailValid(email: String): Boolean {
    val emailRegex = "^[a-z0-9]+@[a-z0-9]+\\.[a-z]{2,3}$".toRegex()
    return email.matches(emailRegex)
}

fun isPasswordValid(pass: String): Boolean {
    val passRegex = "^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d)(?=.*[\\s!@#\$%^&*()\\-_=+]).{8,}$".toRegex()
    return pass.matches(passRegex)
}
```

## 29. Смена темы (Светлая/Темная)
```kotlin
// Включить темную тему
AppCompatDelegate.setDefaultNightMode(AppCompatDelegate.MODE_NIGHT_YES)

// Включить светлую тему
AppCompatDelegate.setDefaultNightMode(AppCompatDelegate.MODE_NIGHT_NO)

// Следовать системной теме
AppCompatDelegate.setDefaultNightMode(AppCompatDelegate.MODE_NIGHT_FOLLOW_SYSTEM)
```

## 30. Работа с камерой и галереей (Модуль Д)
Современный способ через `ActivityResultLauncher`:
```kotlin
// Для галереи
val pickVisualMedia = registerForActivityResult(ActivityResultContracts.PickVisualMedia()) { uri ->
    if (uri != null) {
        // У тебя есть ссылка на фото, передавай её на экран Resize
    }
}
// Вызов: pickVisualMedia.launch(PickVisualMediaRequest(ActivityResultContracts.PickVisualMedia.ImageOnly))

// Для камеры (нужен FileProvider, если сложно — надейся на галерею)
```

## 31. Локальное уведомление через 1 минуту (Модуль Д)
Это делается через `WorkManager`.

**1. Создай Worker:**
```kotlin
class NotificationWorker(context: Context, params: WorkerParameters) : Worker(context, params) {
    override fun doWork(): Result {
        // Код создания уведомления (NotificationCompat.Builder)
        return Result.success()
    }
}
```
**2. Запусти его при выходе из приложения (в onStop):**
```kotlin
val request = OneTimeWorkRequestBuilder<NotificationWorker>()
    .setInitialDelay(1, TimeUnit.MINUTES)
    .addTag("notify_tag")
    .build()
WorkManager.getInstance(this).enqueueUniqueWork("notify", ExistingWorkPolicy.REPLACE, request)
```

## 32. Event Bus для Web-сокетов (Модуль А)
Самая простая реализация через Kotlin `Object` и `SharedFlow`:
```kotlin
object EventBus {
    private val _events = MutableSharedFlow<String>() // Стринг или твой класс данных
    val events = _events.asSharedFlow()

    suspend fun emit(event: String) {
        _events.emit(event)
    }
}

// В Activity/Fragment:
lifecycleScope.launch {
    EventBus.events.collect { event ->
        // Реакция на событие (например, пришло сообщение из сокета)
    }
}
```

## 33. TensorFlow Lite (Базовый запуск)
Тебе дадут модель `.tflite`. Пример запуска:
```kotlin
// Подключи библиотеку: implementation("org.tensorflow:tensorflow-lite:2.14.0")
val interpreter = Interpreter(loadModelFile(assets, "model.tflite"))

// Пример вызова (очень упрощенно):
val input = FloatArray(100) // данные картинки
val output = Array(1) { FloatArray(10) } // вероятности 10 классов
interpreter.run(input, output)
```

## 34. Виджет (Модуль Д)
Создай `AppWidgetProvider`. Главный метод:
```kotlin
override fun onUpdate(context: Context, appWidgetManager: AppWidgetManager, appWidgetIds: IntArray) {
    for (id in appWidgetIds) {
        val views = RemoteViews(context.packageName, R.layout.my_widget_layout)
        views.setTextViewText(R.id.widget_text, "Ваш рейтинг: 5")
        appWidgetManager.updateAppWidget(id, views)
    }
}
```

## 35. Тестирование (Модуль Е: TDD)
Тебе нужно будет написать тест **до** того, как напишешь код.
```kotlin
class OnboardingTest {
    @Test
    fun testQueueOrder() {
        val queue = MyQueue(listOf("1", "2"))
        assertEquals("1", queue.next()) // Проверка, что первый элемент верный
        assertEquals(1, queue.size())   // Проверка, что размер уменьшился
    }
}
```

---

## 36. Onboarding (Слайды)
Для этого экрана в Android лучше всего использовать `ViewPager2`.

**Верстка (Activity/Fragment):**
```xml
<androidx.viewpager2.widget.ViewPager2
    android:id="@+id/viewPager"
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:layout_weight="1" />
```
**Код переключения по кнопке "Next":**
```kotlin
binding.btnNext.setOnClickListener {
    val current = binding.viewPager.currentItem
    if (current < adapter.itemCount - 1) {
        binding.viewPager.currentItem = current + 1
    } else {
        // Если слайды кончились — переход на выбор языка
        startActivity(Intent(this, LanguageActivity::class.java))
    }
}
```

## 37. Таймер обратного отсчета (Для экрана «Игра»)
На макете есть поиск игрока с таймером.
```kotlin
val timer = object : CountDownTimer(10000, 1000) {
    override fun onTick(millisUntilFinished: Long) {
        binding.tvTimer.text = "Search: ${millisUntilFinished / 1000}s"
    }

    override fun onFinish() {
        // Если никто не нашелся — запускаем логику с "ботом"
        startBotGame()
    }
}.start()
```

## 38. Пульсирующая кнопка (Для Аудирования)
В задании сказано: «кнопка должна плавно менять размер, создавая эффект пульсации».
```kotlin
val pulseAnimation = ObjectAnimator.ofPropertyValuesHolder(
    binding.btnMicrophone,
    PropertyValuesHolder.ofFloat("scaleX", 1.2f),
    PropertyValuesHolder.ofFloat("scaleY", 1.2f)
).apply {
    duration = 500
    repeatCount = ObjectAnimator.INFINITE
    repeatMode = ObjectAnimator.REVERSE
}

// Запуск при нажатии:
pulseAnimation.start()
// Остановка:
pulseAnimation.cancel()
binding.btnMicrophone.scaleX = 1f
binding.btnMicrophone.scaleY = 1f
```

## 39. Сетка упражнений (GridLayout)
На главном экране упражнения расположены плиткой. Проще всего это сделать через `RecyclerView` с `GridLayoutManager`.
```kotlin
binding.recyclerView.layoutManager = GridLayoutManager(this, 2) // 2 колонки
```

## 40. Статусы кнопок (Правильно/Неправильно)
На экранах с тестами кнопки меняют цвет (Зеленый/Оранжевый).
```kotlin
fun checkAnswer(isCorrect: Boolean, button: Button) {
    if (isCorrect) {
        button.setBackgroundColor(Color.GREEN)
        // Начисляем баллы: 1 + 0.2 * серия
    } else {
        button.setBackgroundColor(Color.parseColor("#FF5722")) // Оранжевый
        // Сброс серии
    }
}
```

## 41. Цвета из макета (добавь в `res/values/colors.xml`)
Судя по фигме, основные цвета такие (примерные HEX):
```xml
<color name="main_purple">#4A148C</color>
<color name="accent_blue">#2196F3</color>
<color name="error_orange">#FF5722</color>
<color name="success_green">#4CAF50</color>
<color name="background_light">#F5F5F5</color>
```

## 42. Заголовки для Supabase (Retrofit Headers)
Supabase требует передавать `apikey` и `Authorization`. Чтобы не писать это в каждом запросе, добавь это в `RetrofitInstance`:
```kotlin
// В объекте RetrofitInstance добавь перехватчик (Interceptor):
val client = OkHttpClient.Builder().addInterceptor { chain ->
    val request = chain.request().newBuilder()
        .addHeader("apikey", "ТВОЙ_КЛЮЧ")
        .addHeader("Authorization", "Bearer ТВОЙ_КЛЮЧ")
        .build()
    chain.proceed(request)
}.build()

// И добавь .client(client) в Retrofit.Builder()
```

## 43. Сохранение настроек (SharedPreferences)
Для задач: «сохранить выбранный язык», «сохранить сессию входа», «сохранить состояние Onboarding».
```kotlin
val prefs = getSharedPreferences("settings", Context.MODE_PRIVATE)

// Сохранить
prefs.edit().putString("lang", "en").apply()
prefs.edit().putBoolean("is_logged_in", true).apply()

// Прочитать
val lang = prefs.getString("lang", "ru") // "ru" - значение по умолчанию
val isLoggedIn = prefs.getBoolean("is_logged_in", false)
```

## 44. Разрешения (Permissions) для Android 13+
В Android 13 (API 33) разрешения на уведомления и фото запрашиваются отдельно!
```xml
<!-- В AndroidManifest.xml -->
<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
<uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
```
**Код запроса:**
```kotlin
val requestPermissionLauncher = registerForActivityResult(ActivityResultContracts.RequestPermission()) { isGranted ->
    if (isGranted) { /* Ок */ } else { /* Отказ */ }
}
// Вызов: requestPermissionLauncher.launch(Manifest.permission.POST_NOTIFICATIONS)
```

## 45. Навигация между фрагментами (Navigation Component)
Если в проекте используются Фрагменты, навигация делается так:
```kotlin
// Переход
findNavController().navigate(R.id.action_list_to_detail)

// Переход с данными (Bundle)
val bundle = bundleOf("id" to 10)
findNavController().navigate(R.id.action_list_to_detail, bundle)

// Получение данных
val id = arguments?.getInt("id")
```

## 46. Программная смена языка (Локализация)
В задании просят менять язык в приложении. Самый простой способ:
```kotlin
fun setLocale(langCode: String) {
    val locale = Locale(langCode)
    Locale.setDefault(locale)
    val config = resources.configuration
    config.setLocale(locale)
    resources.updateConfiguration(config, resources.displayMetrics)
    // Чтобы применилось, нужно перезапустить Activity:
    recreate()
}
```

---

### 🏁 Финальный совет перед стартом:

1.  **Проверь `AndroidManifest.xml`**: Убедись, что все Activity там прописаны, а интернет разрешен: `<uses-permission android:name="android.permission.INTERNET" />`.
2.  **ID в XML**: Давай элементам понятные ID (например, `btnSignUp`, `tvEmailError`). Это сэкономит время в Биндинге.
3.  **Supabase Auth**: Если будешь делать регистрацию через Supabase, помни, что это просто POST запрос на `/auth/v1/signup` с телом `{email, password}`.

1. Сохрани этот текст в файл `COMPETITION_GUIDE.md`.
2. Сделай `git add .` и `git commit -m "Final cheat sheet updated"`.
3. Сделай `git push`, чтобы она была на GitHub (вдруг пригодится посмотреть с другого компа в те 10 минут инета).

**Ты сделала всё возможное. Завтра просто выдохни и пиши код. Удачи на конкурсе!** 🏆🔥

### 💡 Важные советы по макетам из фигмы:

1.  **Splash Screen (Анимация):** Если дадут видеофайл, используй `VideoView`. Если просто картинку — сделай Activity, которая через 2 секунды (`Handler().postDelayed`) закрывается и открывает Onboarding.
2.  **Обрезка фото (Profile Resize):** Это самое сложное. Если времени мало, попробуй найти готовую библиотеку в проекте или просто используй `ImageView.setScaleType(ScaleType.CENTER_CROP)`. 
3.  **Адаптивность:** На скринах много элементов по центру. Используй `ConstraintLayout` и его «цепи» (Chains), чтобы элементы не разъезжались на разных экранах. 
    *   *Лайфхак:* Используй `app:layout_constraintVertical_bias="0.3"`, чтобы поднять блок чуть выше центра (как на экране Login).

### Как организовать "серию побед" (Баллы):
Создай глобальную переменную в Activity:
```kotlin
var streakCount = 0
var totalScore = 0.0

fun onCorrectAnswer() {
    streakCount++
    val bonus = if (streakCount >= 2) 0.2 * streakCount else 0.0
    totalScore += (1 + bonus)
}

fun onWrongAnswer() {
    streakCount = 0
}
```

### Что еще важно взять из ТЗ:
1.  **Supabase:** Это облачная база. В задании сказано «только через REST API». Это значит, ты работаешь с ней как с обычным **Retrofit**, который мы уже записали.
2.  **Адаптивная верстка:** Не используй фиксированные `width` и `height` в пикселях (только `0dp` с Constraints или `wrap_content`).
3.  **Splash Screen:** В Android 13+ есть специальное API для сплэшей, но проще сделать отдельную Activity с анимацией.
4.  **Таймер:** Для поиска игрока (10 сек) используй `object : CountDownTimer(10000, 1000) { ... }`.

**Совет по Figma:** Поскольку интернета не будет, в сессии с интернетом (10 мин) **выпиши себе все цвета (HEX), размеры шрифтов и отступы** или скачай все картинки сразу.

### ⚠️ Важное дополнение: Как это всё запустить (Coroutines)
На скринах препода функции помечены словом **`suspend`**. Это значит, их нельзя просто так вызвать, программа упадет. Их нужно запускать в "корутине".

Добавь себе в шпору пример вызова из Activity:

```kotlin
import androidx.lifecycle.lifecycleScope
import kotlinx.coroutines.launch

// Внутри onCreate:
lifecycleScope.launch {
    try {
        // Вызываем сетевой запрос из RetrofitInstance
        val response = RetrofitInstance.api.getPhotos()
        // Тут обновляем адаптер
        myAdapter.items = response
    } catch (e: Exception) {
        // Если нет интернета или ошибка
        Log.e("ERROR", e.message.toString())
    }
}
```

### Почему это важно для конкурса?
1. **RecyclerView:** На конкурсе 100% будет задание «вывести список». Этот код — 50% успеха.
2. **Glide:** Если нужно показать картинку по ссылке, используй одну строку: `Glide.with(context).load(url).into(imageView)`. (В коде препода его нет, но в библиотеках он есть!)
3. **Data Class:** Не забудь создать сам объект данных (например, `data class Product(val title: String, val price: Int)`), иначе адаптеру нечего будет отображать.

Теперь у тебя в шпаргалке есть **полный цикл**: как настроить проект, как скачать данные, как их сохранить и как их показать в списке. **Это и есть скелет приложения.**

### 🛠️ Что делать, если Gradle "сломался" или "висит"?
Без интернета Gradle может начать капризничать. 

1.  **File -> Settings -> Build, Execution, Deployment -> Build Tools -> Gradle**.
2.  Поставь галочку **"Offline mode"**. Это заставит Студию использовать только те файлы, которые уже скачаны.
3.  Если проект не собирается, попробуй **Build -> Clean Project**, а затем **Build -> Rebuild Project**.

### 💡 Лайфхак для конкурса
Если ты понимаешь, что не знаешь, как реализовать какую-то логику, **используй логи**.
`Log.d("CHECK", "Я дошла до этого места, значение x = $x")`
Даже если на экране ничего не меняется, ты увидишь в окне **Logcat**, работает ли твоя логика вообще.

### 🔥 Секретные фишки Android Studio 

1.  **`Ctrl + Alt + T` (Surround With):**
    Выдели кусок кода и нажми это. Студия предложит обернуть его в `if`, `for`, `while` или `try-catch`. Очень ускоряет написание конструкций.
2.  **`Ctrl + D`:**
    Дублировать текущую строку.
3.  **`Ctrl + Y`:**
    Удалить текущую строку.
4.  **`Ctrl + F12`:**
    Показывает список всех функций в текущем файле. Если файл огромный, так проще перемещаться.
5.  **Логи (Logcat):**
    Если что-то не работает в Android, пиши:
    `Log.d("MY_TAG", "Значение переменной: $x")`
    Потом внизу в окне **Logcat** ищи по тегу `MY_TAG`.

В Android Studio есть **"Live Templates"**. 
*   Напиши `logd` и нажми `Tab` — она сама создаст строку для лога.
*   Напиши `toast` и нажми `Tab` — она создаст всплывающее сообщение.

# ⌨️ ГОРЯЧИЕ КЛАВИШИ 

1.  **`Alt + Enter`** — **ГЛАВНАЯ КНОПКА**. Исправляет ошибки, импортирует библиотеки, предлагает упростить код.
2.  **`Ctrl + Space`** — Показать список доступных функций/переменных.
3.  **`Ctrl + P`** — Показать, какие параметры принимает функция (внутри скобок).
4.  **`Ctrl + Q`** — Быстрая документация (что делает эта функция).
5.  **`Ctrl + Alt + L`** — Выровнять код (сделать его красивым).
6.  **`Shift + Shift`** — Поиск вообще всего в проекте.
7.  **`Ctrl + /`** — Закомментировать/раскомментировать строку.
8.  **`Alt + Insert`** — Быстро создать конструктор, методы или `toString()`.

### 1. Где взять офлайн документацию?

Поскольку скачивать сторонние программы (типа Zeal или Dash) тебе, скорее всего, запретят, твой главный источник знаний — **сама Android Studio**.

*   **Quick Documentation (Ctrl + Q):** Наведи на любую функцию или класс и нажми это. Студия покажет описание, параметры и возвращаемые типы прямо из исходников, которые уже скачаны вместе с SDK. Это и есть твоя «библия».
*   **External Documentation (Shift + F1):** Если документация была скачана вместе с SDK, это сочетание откроет её в браузере (локально, без интернета).
*   **Исходный код (Ctrl + Click):** Самый надежный способ. Проваливайся внутрь функций библиотек (Retrofit, Room, Glide). Там всегда есть подробные комментарии над методами, объясняющие, как ими пользоваться.
*   **Kotlin Документация:** В Android Studio встроен плагин Kotlin, и вся стандартная библиотека (списки, строки, математика) доступна для просмотра через Ctrl+Click даже без сети.

---

### 2. Как работает Git без интернета?

В этом и есть главная фишка Git! **Git — это распределенная система.**

*   **Локальный репозиторий:** Когда ты пишешь `git commit`, тебе **НЕ нужен интернет**. Все изменения сохраняются в папку `.git` на твоем компьютере. Ты можешь создавать ветки, делать коммиты и смотреть историю полностью оффлайн.
*   **Удаленный репозиторий (GitHub/GitLab):** Интернет нужен только для команд `push` (отправить) и `pull` (забрать).

**Как это будет на конкурсе:**
Скорее всего, сервер GitLab находится в **локальной сети** конкурса. Это значит, что `push` и `pull` будут работать всегда, даже если «внешнего» интернета (Гугла) нет. Если же GitLab внешний, то ты делаешь коммиты оффлайн, а в те 10 минут интернета просто пишешь `git push`.

---

### 3. Шпаргалка по Git (Самые нужные команды)

На конкурсе тебе важно следовать веткам и делать правильные сообщения (RED, GREEN, REFACTOR для TDD).

#### Базовый цикл (делать каждые 15–30 минут):
1.  `git status` — проверить, какие файлы изменились.
2.  `git add .` — подготовить все изменения к сохранению.
3.  `git commit -m "RED: реализовал тесты для Onboarding"` — сохранить изменения локально (сообщение пиши по заданию!).

#### Работа с ветками (если просят сессии Path-X):
*   `git branch` — посмотреть список веток.
*   `git checkout -b Path-1` — создать новую ветку для 1-й сессии и перейти в неё.
*   `git checkout main` — вернуться на главную ветку.
*   `git merge Path-1` — объединить ветку сессии с главной (делать в конце).

#### Работа с удаленным сервером (в те самые 10 минут):
*   `git remote add origin ССЫЛКА` — привязать репозиторий (делается один раз в начале).
*   `git push origin Path-1` — отправить ветку на сервер.
*   `git pull origin main` — забрать изменения с сервера (если работаешь не одна).

#### Если что-то пошло не так (Спасение):
*   `git log --oneline` — посмотреть список последних коммитов (кратко).
*   `git checkout .` — **ОПАСНО:** отменить все незакоммиченные изменения в текущей папке (вернуть как было на моменте последнего коммита).
*   `git commit --amend -m "Новое сообщение"` — исправить текст последнего коммита (если опечаталась).

---

### Итог для твоей «супер-шпоры»:

Добавь этот блок команд Git, чтобы не вспоминать их в панике:

```bash
# Начало работы
git init
git remote add origin https://github.com/Fizzy423/MyTest.git
git checkout -b Path-1

# Сохранение (оффлайн)
git add .
git commit -m "RED: описание"

# Отправка (когда дали интернет)
git push origin Path-1

# Исправление ошибок
git log             # Посмотреть историю
git reset --hard    # Сбросить всё к чертям до последнего коммита (аккуратно!)
```

**Главный совет:** Не бойся 10-минутного лимита. Вся твоя работа — это написание кода. А для этого интернет не нужен. Используй эти 10 минут только чтобы:
1. Загуглить конкретную ошибку (если `Alt+Enter` не помог).
2. Скачать картинку/иконку из Фигмы, если забыла.
3. Сделать `push` в репозиторий.

**Ты готова. Удачи!**
### Что делать, если все забыла:
1. Пиши название переменной, ставь точку и жми **`Ctrl + Space`**. Студия сама покажет все, что можно с ней сделать.
2. Если код горит красным, наводи курсор и жми **`Alt + Enter`**.
3. Не помнишь, как работает функция? Зажми **`Ctrl`** и кликни по ней мышкой — ты перейдешь в её исходный код, там часто есть примеры в комментариях.

---

### Шаг 1: Подготовка файла приложения (APK)
Магазин принимает не код, а скомпилированный файл. Тебе нужно создать **Signed APK** (подписанный файл).

1.  В Android Studio: **Build -> Generate Signed Bundle / APK...**
2.  Выбери **APK** -> Next.
3.  **Key store path:** Нажми *Create new...* (если ключа еще нет).
    *   Придумай пароль (запиши его!), укажи имя (например, `medic_key`).
    *   Заполни любое поле (например, First and Last Name). Нажми OK.
4.  Выбери **release** вариант сборки.
5.  Нажми **Finish**. Студия соберет файл. В конце выскочит уведомление «APK(s) generated successfully» — нажми *Locate*, чтобы найти файл в папке. **Этот файл ты будешь загружать в RuStore.**

---

### Шаг 2: Работа в консоли RuStore
Тебе дадут логин/пароль. Заходишь и нажимаешь **«Добавить приложение»**.

1.  **Название:** По заданию: `ТвояФамилияMedic` (например, `Fmedic`).
2.  **Загрузка:** Перетаскиваешь туда свой полученный на Шаге 1 APK-файл.
3.  **Иконка:** Загружаешь картинку из Фигмы (обычно 512x512 px, формат PNG).
4.  **Категория:** Выбирай «Медицина» или «Образование» (так как это обучение языку для медиков).
5.  **Возрастной рейтинг:** Ставь `0+` или `3+` (если в приложении нет ничего взрослого).
6.  **Описания:**
    *   *Краткое:* «Приложение для обучения медицинскому английскому».
    *   *Полное:* Распиши функции: «Регистрация, личный профиль, упражнения на карточках, распознавание животных через AI, соревновательный режим».

---

### Шаг 3: Скриншоты (Важно!)
Магазин не пустит без скриншотов.
*   **Где взять:** Запусти приложение на эмуляторе и нажми иконку фотоаппарата на панели эмулятора. Сделай 3–5 штук: Экран входа, Главная, Экран упражнения.
*   **Требование:** В RuStore обычно нужны скриншоты вертикальной ориентации.

---

### Шаг 4: Презентация (Для разработчиков)
Предупреждение: **НЕ делай рекламу!** Эксперты — технари, им плевать на красоту кнопок, им важен «движок».

**Что должно быть в слайдах:**
1.  **Архитектура:** Напиши: «Использовалась архитектура MVVM (или та, что была в задании). Слои разделены на Data, Domain и UI».
2.  **Схема классов:** Можешь просто перечислить основные классы: `ApiService`, `Database`, `ProductAdapter`, `Repository`.
3.  **Библиотеки:** Напиши список и **зачем**:
    *   *Retrofit + GSON* — для работы с API Supabase.
    *   *Room* — для оффлайн-кэширования данных.
    *   *Glide* — для загрузки изображений.
    *   *Coroutines* — для асинхронных запросов.
4.  **Производительность:** Напиши, что используешь `RecyclerView` (для экономии памяти в списках) и `diffUtil` (для быстрой отрисовки).

---

### Дополнительные советы по Модулю Ж:

*   **Git Merge:** Не забудь в конце сделать слияние. В терминале:
    ```bash
    git checkout main
    git merge Path-1  # вместо Path-1 имя твоей сессии
    git push origin main
    ```
*   **Соблюдай нейминг:** Если в задании сказано `Фамилия + medic`, пиши ровно так. Ошибки в названии — потеря баллов.
*   **Бесплатное:** В настройках цены в RuStore обязательно убедись, что стоит галочка «Бесплатно».

**Твой план на этот модуль:**
Сначала за 15 минут собери APK. Потом за 30 минут заполни всё в RuStore. Оставшееся время трать на презентацию и проверку Git. 

